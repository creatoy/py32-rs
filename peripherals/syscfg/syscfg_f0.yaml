SYSCFG:
  CFGR1:
    MEM_MODE:
      MainFlash: [0, "Main Flash memory mapped at 0x0000_0000"]
      SystemFlash: [1, "System Flash memory mapped at 0x0000_0000"]
      MainFlash2: [2, "Main Flash memory mapped at 0x0000_0000"]
      SRAM: [3, "Embedded SRAM mapped at 0x0000_0000"]
    "I2C_P?*_ANF":
      Standard: [0, "Pin operate in standard mode"]
      AnalogFilter: [1, "I2C Analog filter enabled on pin"]
  CFGR2:
    ETR_SRC_TIM1:
      GPIO: [0, "TIM1_ETR is connected to GPIO"]
      COMP1: [1, "TIM1_ETR is connected to COMP1"]
      COMP2: [2, "TIM1_ETR is connected to COMP2"]
      ADC: [3, "TIM1_ETR is connected to ADC"]
    "COMP?_BRK_TIM*":
      Disconnected: [0, "COMP? output not connect to TIM? Break input"]
      Connected: [1, "COMP? output connected to TIM? Break input"]
    PVD_LOCK:
      Disconnected: [0, "PVD LOCKUP output disconnected from TIM1/16/17 Break input"]
      Connected: [1, "PVD LOCKUP output connected to TIM1/16/17 Break input"]
    LOCKUP_LOCK:
      Disconnected: [0, "Cortex-M0+ LOCKUP output disconnected from TIM1/16/17 Break input"]
      Connected: [1, "Cortex-M0+ LOCKUP output connected to TIM1/16/17 Break input"]
  CFGR3:
    "DMA?_MAP":
      ADC: [0, "ADC DMA requests mapped on DMA?"]
      SPI1_TX: [1, "SPI1_TX DMA requests mapped on DMA?"]
      SPI1_RX: [2, "SPI1_RX DMA requests mapped on DMA?"]
      USART1_TX: [5, "USART1_TX DMA requests mapped on DMA?"]
      USART1_RX: [6, "USART1_RX DMA requests mapped on DMA?"]
      USART2_TX: [7, "USART2_TX DMA requests mapped on DMA?"]
      USART2_RX: [8, "USART2_RX DMA requests mapped on DMA?"]
      I2C_TX: [9, "I2C_TX DMA requests mapped on DMA?"]
      I2C_RX: [10, "I2C_RX DMA requests mapped on DMA?"]
      TIM1_CH1: [11, "TIM1_CH1 DMA requests mapped on DMA?"]
      TIM1_CH2: [12, "TIM1_CH2 DMA requests mapped on DMA?"]
      TIM1_CH3: [13, "TIM1_CH3 DMA requests mapped on DMA?"]
      TIM1_CH4: [14, "TIM1_CH4 DMA requests mapped on DMA?"]
      TIM1_COM: [15, "TIM1_COM DMA requests mapped on DMA?"]
      TIM1_UP: [16, "TIM1_UP DMA requests mapped on DMA?"]
      TIM1_TRIG: [17, "TIM1_TRIG DMA requests mapped on DMA?"]
      TIM3_CH1: [18, "TIM3_CH1 DMA requests mapped on DMA?"]
      TIM3_CH3: [19, "TIM3_CH3 DMA requests mapped on DMA?"]
      TIM3_CH4: [20, "TIM3_CH4 DMA requests mapped on DMA?"]
      TIM3_TRG: [21, "TIM3_TRG DMA requests mapped on DMA?"]
      TIM3_UP: [22, "TIM3_UP DMA requests mapped on DMA?"]
      TIM16_CH1: [24, "TIM16_CH1 DMA requests mapped on DMA?"]
      TIM16_UP: [25, "TIM16_UP DMA requests mapped on DMA?"]
      TIM17_CH1: [26, "TIM17_CH1 DMA requests mapped on DMA?"]
      TIM17_UP: [27, "TIM17_UP DMA requests mapped on DMA?"]